"    if (!form) return;\n\n    // Address Mode Toggle Logic\n    let addressMode = 'manual';\n    const addressModeBtns = modal.querySelectorAll('.address-mode-btn');\n    const addressInputWrapper = document.getElementById('addressInputWrapper');\n    const manualContainer = document.getElementById('orderAddressManualContainer');\n    const gpsContainer = document.getElementById('orderAddressGpsContainer');\n    const manualInput = document.getElementById('orderAddressManual');\n    const gpsInput = document.getElementById('orderAddressGps');\n\n    addressModeBtns.forEach(btn => {\n        btn.addEventListener('click', () => {\n            addressModeBtns.forEach(b => b.classList.remove('active'));\n            btn.classList.add('active');\n            \n            const mode = btn.dataset.mode;\n            addressMode = mode;\n            \n            if (mode === 'manual') {\n                if (manualContainer) manualContainer.style.display = 'block';\n                if (gpsContainer) gpsContainer.style.display = 'none';\n                if (manualInput) manualInput.required = true;\n                if (gpsInput) gpsInput.required = false;\n                if (addressInputWrapper) addressInputWrapper.className = 'address-input-wrapper manual-active';\n            } else {\n                if (manualContainer) manualContainer.style.display = 'none';\n                if (gpsContainer) gpsContainer.style.display = 'block';\n                if (manualInput) manualInput.required = false;\n                if (gpsInput) gpsInput.required = true;\n                if (addressInputWrapper) addressInputWrapper.className = 'address-input-wrapper gps-active';\n            }\n        });\n    });\n\n    // GPS Location Logic\n    const getGpsBtn = document.getElementById('orderGetGpsBtn');\n    const gpsStatus = document.getElementById('gpsStatus');\n\n    if (getGpsBtn) {\n        getGpsBtn.addEventListener('click', () => {\n            if (!navigator.geolocation) {\n                if (gpsStatus) {\n                
<truncated 9698 bytes>