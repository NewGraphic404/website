# Website Optimization Guide

هذا الدليل يشرح كيفية تحسين حجم الموقع من 4.32 GB إلى ~700 MB

---

## الخطوة 1: حذف الملفات غير المستخدمة (3 دقائق)

### التشغيل:
```powershell
.\cleanup-unused-files.ps1
```

### ماذا يفعل:
- يفحص كل الصور والفيديوهات في مجلد `assets`
- يتحقق من استخدامها في `index.html`, `styles.css`, `script.js`
- يحذف الملفات غير المستخدمة (443 ملف = 560 MB)
- يحفظ قائمة بالملفات المحذوفة في ملف log

### النتيجة:
- توفير: **560 MB**
- الوقت: **~3 دقائق**

---

## الخطوة 2: ضغط الصور (45 دقيقة)

### المتطلبات:
يجب تثبيت **ImageMagick** أولاً:

1. حمّل من: https://imagemagick.org/script/download.php#windows
2. اختر: `ImageMagick-7.x.x-Q16-HDRI-x64-dll.exe`
3. أثناء التثبيت، تأكد من تفعيل: ✅ **"Add application directory to system path"**
4. أعد تشغيل PowerShell

### التشغيل:
```powershell
.\compress-images.ps1
```

### ماذا يفعل:
- يعمل نسخة احتياطية من كل الصور في مجلد `assets-backup-[timestamp]`
- يضغط كل الصور (2290 صورة):
  - الحد الأقصى للعرض: 2000 بكسل (كافي لشاشات 4K)
  - الجودة: 85% (جودة عالية للويب)
  - يحذف metadata الزائدة
- يحفظ الصور المضغوطة في نفس المكان

### النتيجة:
- توفير متوقع: **~3 GB**
- الوقت: **20-40 دقيقة**
- الجودة: **لا فرق ملحوظ للعين**

---

## الخطوة 3 (اختيارية): ضغط الفيديوهات

الفيديوهات تحتاج أداة منفصلة. استخدم **HandBrake**:

1. حمّل من: https://handbrake.fr/downloads.php
2. افتح الفيديو في HandBrake
3. اختر Preset: **"Web" → "Gmail Large 3 Minutes 720p30"**
4. اضغط "Start Encode"

### الفيديوهات الحالية:
- `about us.mp4`: 53 MB → يمكن ضغطه لـ ~8 MB
- `lv_0_20260514120337.mp4`: 20 MB → يمكن ضغطه لـ ~4 MB

---

## النتيجة النهائية المتوقعة

| البند | قبل | بعد | التوفير |
|------|-----|-----|---------|
| الملفات غير المستخدمة | 560 MB | 0 MB | 560 MB |
| الصور | ~3.5 GB | ~500 MB | 3 GB |
| الفيديوهات | 73 MB | ~12 MB | 61 MB |
| **المجموع** | **4.32 GB** | **~700 MB** | **~3.6 GB (83%)** |

---

## طريقة بديلة (بدون ImageMagick)

إذا لم تستطع تثبيت ImageMagick، استخدم أدوات أونلاين:

### 1. TinyPNG (للصور):
- اذهب إلى: https://tinypng.com
- ارفع حتى 20 صورة في المرة الواحدة
- حمّل الصور المضغوطة
- استبدلها في مجلد `assets`

### 2. Squoosh (للصور):
- اذهب إلى: https://squoosh.app
- ارفع صورة واحدة
- اختر: **MozJPEG** بجودة **85**
- حمّل الصورة المضغوطة

---

## استعادة النسخة الأصلية

إذا أردت استعادة الصور الأصلية:

```powershell
# احذف المجلد المضغوط
Remove-Item "assets" -Recurse -Force

# استعد النسخة الاحتياطية
Copy-Item "assets-backup-[timestamp]/assets" "." -Recurse
```

---

## ملاحظات مهمة

✅ **النسخ الاحتياطية**:
- السكريبتات تعمل نسخ احتياطية تلقائياً
- لا تحذف مجلدات `assets-backup-*` حتى تتأكد من النتيجة

✅ **الجودة**:
- الضغط بجودة 85% لا يؤثر على الجودة البصرية
- الصور ستبقى واضحة تماماً على الشاشة

✅ **السرعة**:
- الموقع سيكون أسرع بكثير للزوار
- Google سيحسن ترتيب موقعك في نتائج البحث

---

## الدعم

إذا واجهت أي مشكلة:
1. تأكد من تثبيت ImageMagick بشكل صحيح
2. أعد تشغيل PowerShell بعد التثبيت
3. تأكد من تشغيل PowerShell كـ Administrator
4. استخدم الطريقة البديلة (أدوات أونلاين) إذا استمرت المشكلة
